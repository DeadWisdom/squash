{
  "name": "squash",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mkdir -p reports; cucumber-js --require-module ts-node/register --publish-quiet --format @cucumber/pretty-formatter --format summary --format html:reports/report.html --format json:reports/report.json  --require tests/**/*.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^7.3.1",
    "@cucumber/pretty-formatter": "^1.0.0-alpha.1",
    "@playwright/test": "^1.15.0",
    "expect": "^27.2.1",
    "ts-node": "^10.2.1"
  }
}
